# gitrep

Analysis project. Python. Mamba env `gitrep`. Test strategy: test-along.

## claude-workflows

Config: `.claude/project-config.yml`
Specs: `specs/` (mutable, git-tracked)

Next: `/new-spec analysis` to scaffold first spec.

Conventions: see `python-analysis-conventions` skill (xarray, env mgmt, test discipline, reproducibility manifest).
